Project Context: Video Game Recommendation Engine

    Core Stack: Python, Pandas/Numpy for data, Scikit-learn or PyTorch for similarity modeling.

    Data Handling: Prioritize vector similarity (Cosine/Euclidean) for game recommendations.

    Constraints:

        Keep files under 300 lines; refactor into modules if they grow larger.

        Always use absolute paths when referencing project files to avoid tool-calling errors.

        When suggesting new games, reference the internal "popularity slider" logic discussed in previous sessions.